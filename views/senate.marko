<!DOCTYPE html>

var meta=data.model.metadata

html lang="en"
  head
    meta charset="utf-8"
    title - ${data.model.metadata.title}
    meta name="viewport" content="width=device-width, initial-scale=1"
    link rel="stylesheet" href="${data.path_to_asset('scss', 'stylesheets/index.css')}"
    $!{data.render_template('_social-meta', data)}
  body.senate
    $!{data.render_template('_nav', data)}

    $!{data.render_template('_senate-header', data)}

    article
      $!{data.render_template('_senate-summary', data)}

      $!{data.render_template('_senate-seat-counts', data)}

      $!{data.render_template('_senate-races', data)}

      h2.with-toc-link#methodology
        a href="#methodology" - Methodology

      $!{data.model.render(data)}

      <!--
      .comments
        .fb-comments data-width="100%"
      -->

    $!{data.render_template('_footer', data)}

    script src="${data.path_to_asset('digest', 'javascripts/social.js')}"
    script src="${data.path_to_asset('digest', 'javascripts/stats.js')}"
    script src="${data.path_to_asset('digest', 'javascripts/senate.js')}"
