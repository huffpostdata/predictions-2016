.key.${data.model}
  .label
    h5 - Key
  .container data-xibar="${data.model == 'likely-clinton' ? 15 : (data.model == 'battlegrounds' ? 0 : -15)}" data-stddev="${data.model == 'battlegrounds' ? 6 : 3}"
    .mean - Average outcome
    canvas
    .chance - 95% of outcomes
