Run `generate.js` on your dev machine. The reason we run it on dev machines is
that it's easier to configure the live servers.

See https://github.com/Automattic/node-canvas for installation instructions.
